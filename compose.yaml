services:
  dispatch:
    build: .
    ports: ["127.0.0.1:8080:8080"]
    environment:
      N0DING_DISPATCH_AUTH_TOKEN: ${N0DING_DISPATCH_AUTH_TOKEN:?set N0DING_DISPATCH_AUTH_TOKEN}
    volumes: ["dispatch-data:/data"]
    restart: unless-stopped
volumes:
  dispatch-data: {}
